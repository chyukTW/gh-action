name: TEST_1

on:
  push:
    branches:
      - master
      - 'feat/**'

jobs:
  integration:
    if: github.event_name == 'push'
    name: integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: git_checkout
        uses: actions/checkout@v3

      - name: Setup Node
        id: setup_node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'yarn'

      - name: Install dependencies
        id: install_dependencies
        run: sudo chmod +x ./script.sh "yarn install" "error.log"

      - name: Run test
        id: test
        run: sudo chmod +x ./script.sh "yarn test" "error.log"

      - name: Print
        if: always()
        run: cat error.log
